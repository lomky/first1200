<%= form_for(food) do |f| %>
  <% if food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :meal_plan_id %>
    <%= f.collection_select(:meal_plan_id, MealPlan.all, :id, :name) %>
  </div>

  <div class="field">
    <%= f.label :recipe_id %>
    <%= f.collection_select(:recipe_id, Recipe.all, :id, :name) %>
  </div>

  <div class="field">
    <%= f.label :rating %>
    <%= f.number_field :rating %>
  </div>

  <div class="field">
    <%= f.label :david_rating %>
    <%= f.number_field :david_rating %>
  </div>

  <div class="field">
    <%= f.label :longevity %>
    <%= f.number_field :longevity %>
  </div>

  <div class="field">
    <%= f.label :notes %>
    <%= f.text_field :notes %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>



